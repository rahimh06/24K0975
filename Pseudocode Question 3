# Calculator with + and - 

START

INPUT num1
INPUT num2
INPUT operator

SET Result to 0

IF operator = "+" THEN
    SET Result to num1 + num2
ELSE
    IF operator = "-" THEN
        SET Result to num1 - num2
        PRINT Result
    ELSE
        PRINT "Invalid operator"
END
